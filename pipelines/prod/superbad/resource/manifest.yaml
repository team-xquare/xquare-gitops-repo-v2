---
# Source: applications/templates/poddistruptionbudget.yaml
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  namespace: superbad-prod
  name: superbad-prod
spec:
  maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: superbad-prod
---
# Source: applications/templates/vaultsecret.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: superbad-prod-account
  namespace: superbad-prod
---
# Source: applications/templates/elastic-agent-pvc.yaml
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: build-agent-storage-class
  namespace: gocd
provisioner: ebs.csi.aws.com
parameters:
  type: gp3
  iops: "3000"
  throughput: "125"
  fsType: ext4
reclaimPolicy: Retain
volumeBindingMode: WaitForFirstConsumer
---
# Source: applications/templates/elastic-agent-pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: gocd
  name: superbad-prod-docker-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10G
  storageClassName: build-agent-storage-class
---
# Source: applications/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  namespace: superbad-prod
  name: superbad-prod
  labels:
    app: superbad-prod
    project: superbad
    environment: prod
spec:
  type: ClusterIP
  selector:
    app: superbad-prod
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
    - name: port
      port: 3000
      targetPort: 3000
      protocol: TCP
---
# Source: applications/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: superbad-prod
  name: superbad-prod
  labels:
    app: superbad-prod
    project: superbad
    test: test
    environment: prod
spec:
  replicas: 1
  revisionHistoryLimit: 0
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  selector:
    matchLabels:
      app: superbad-prod
  template:
    metadata:      
      annotations:
        sidecar.istio.io/rewriteAppHTTPProbers: "false"
        sidecar.istio.io/proxyCPU: "5m"
        sidecar.istio.io/proxyMemory: "128Mi"
      labels:
        app: superbad-prod
        project: superbad
    spec:
      serviceAccountName: superbad-prod-account
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: app
                    operator: In
                    values:
                      - superbad-prod
              topologyKey: "kubernetes.io/hostname"
      nodeSelector:
        Karpenter: enabled
      tolerations:
        - effect: "NoSchedule"
          key: xquare/server
          operator: "Equal"
          value: "true"
      terminationGracePeriodSeconds: 120
      containers:
        - name: superbad-prod
          image: 786584124104.dkr.ecr.ap-northeast-2.amazonaws.com/superbad-prod:prod-pi7aeiqi
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          envFrom:
          - secretRef:
              name: superbad-prod
          resources:
            requests:
              memory: "500Mi"
              cpu: "20m"
              ephemeral-storage: "20Mi"
            limits:
              memory: "2Gi"
          readinessProbe:
            tcpSocket:
              port: 3000
            initialDelaySeconds: 20
            periodSeconds: 10
            successThreshold: 3
---
# Source: applications/templates/vaultsecret.yaml
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: superbad-prod-auth
  namespace: superbad-prod
spec:
  method: kubernetes
  mount: kubernetes
  kubernetes:
    role: xquare
    serviceAccount: superbad-prod-account
    audiences:
      - vault
---
# Source: applications/templates/vaultsecret.yaml
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultDynamicSecret
metadata:
  name: superbad-prod-secret
  namespace: superbad-prod
spec:
  mount: xquare-kv
  path: superbad-prod
  destination:
    name: superbad-prod
    create: true
  vaultAuthRef: superbad-prod/superbad-prod-auth
  rolloutRestartTargets:
    - kind: Deployment
      name: superbad-prod
---
# Source: applications/templates/vpa.yaml
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: superbad-prod-vpa
  namespace: superbad-prod
spec:
  targetRef:
    apiVersion: "apps/v1"
    kind:       Deployment
    name:       superbad-prod
  updatePolicy:
    updateMode: "Auto"
  resourcePolicy:
    containerPolicies:
      - containerName: "*"
        controlledResources: [ "memory" ]
---
# Source: applications/templates/virtual-service.yaml
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: superbad-prod-domain-ingress
  labels:
    app.kubernetes.io/instance: xquare-ingress
  namespace: superbad-prod
spec:
  gateways:
    - istio-system/xquare-ingressgateway
  hosts:
    - superbad-server.xquare.app
  http:
    - name: superbad
      route:
        - destination:
            host: 'superbad-prod.superbad-prod.svc.cluster.local'
            port:
              number: 3000
      corsPolicy:
        allowOrigins:
          - regex: ".*"
        allowMethods:
          - POST
          - GET
          - PUT
          - PATCH
          - DELETE
          - OPTIONS
        allowCredentials: true
        allowHeaders:
          - content-type
          - authorization
          - accept
          - accept-encoding
          - Refresh-Token
          - X-Not-Using-Xquare-Auth
          - Oauth-Token
          - oa-token
          - Request-User-Id
          - Request-User-Role
          - X-Refresh-Token
          - x-identifier
        maxAge: "24h"
